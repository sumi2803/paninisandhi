[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "paninisandhi"
version = "0.1.0"
description = "Small toolkit of Paninian sutras as code (sandhi rules, sanjna checks)"
requires-python = ">=3.7"

[tool.setuptools]
packages = ["paninisandhi"]
